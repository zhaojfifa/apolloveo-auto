# Owner Summary — Matrix Script Provider Orchestrator + Multi-Shot Real Result (2026-06-08)

Type: **Heavy batch — runtime engineering + live multi-shot smoke. NOT merged (stopped before merge).**
Full report: `docs/execution/MATRIX_SCRIPT_PROVIDER_ORCHESTRATOR_MULTISHOT_REPORT_20260608.md`.
Evidence: `docs/execution/evidence/matrix_script_provider_multishot_20260608/`.

## What you asked for
A materially better Matrix Script `final.mp4` by orchestrating **Gemini + Akool + ffmpeg**, with the
provider request process **visible to the operator**, multi-shot (≥3 attempts; PASS needs ≥2 real
clips into the final video), results-first.

## What was delivered (proven live, local runtime)
- **3 / 3 target shots generated by the real image_to_video provider, from Gemini-strengthened
  script prompts, and consumed into `final.mp4`** (shot01 海边/场景, shot02 小番茄/产品, shot03
  拿起小番茄/产品). The two semantic-reuse shots fall back honestly. **Exceeds the ≥2 PASS bar.**
- **Final video:** 8.5 MB · 20.0s · 1080×1920 · h264+aac · Azure voiceover audible · subtitles
  burned · QC passed · `official_publish_ready=false`. Real **motion** demonstrated (water splashing
  in the tomato shot — not a panned still). → `final_gemini_3of3.mp4` + motion frames.
- **New operator panel "AI 生成请求过程"** showing per-shot: prompt source (系统生成 / AI 强化 /
  AI 重写重试), status (已生成 / 失败回退), 进入成片 (是/否), 用时, 轮询 trace, fallback reason —
  operator language only, **zero secret/URL/vendor/task-id leak** (4 independent scans clean).
- **Honest by construction:** bounded attempt budget (≤5), Gemini-rewrite-then-retry on failure,
  no silent fallback, no fake success; `schemas/contracts/routes/delivery-truth` untouched; no
  provider selector in the UI; forbidden paths untouched.

## New / changed code (all within your allowed-files list; not merged)
- new `provider_orchestrator.py` (Part A), `gemini_prompt_refiner.py` (Part B)
- `tomato_real_result_orchestrator.py` (multi-shot wiring + panel fold), `akool_image_to_video_capability.py`
  (additive lifecycle hook), `task_workbench.html` (panel), + tests.

## Verdict: PASS WITH ISSUES
The result is operator-usable and materially better **on the local runtime**. Issues are environmental,
not batch defects:
1. **Deployed browser acceptance still impossible** — the Render service is **owner-suspended**
   (HTTP 503), same blocker as your Decision-B audit. (Local template-render evidence provided.)
2. **Deployed `GEMINI_MODEL=gemini-2.0-flash` is decommissioned** (404). The run used `gemini-2.5-flash`.
   Until you update the deployed/`.env` model, Gemini refinement will honestly fall back to
   deterministic in production (still produces a result — verified 3/3 even deterministically).
3. By design, shots 04/05 remain honest backbone fallback ("some fallback remains").

## Decision needed
- **Approve merge** of this batch as a runtime PR (and update `GEMINI_MODEL` so Gemini runs in prod)?
- **Request revision** (shot03 role, target set, panel copy, retry/cost policy)?
- **Continue iteration** (per-shot duration fitting, more shots, BGM, voice controls)?
- Separately, to unblock Decision B: **resume the suspended Render service**, then re-run the
  deploy/browser audit.

*No PR opened. `official_publish_ready=false` preserved. Stopped before merge.*
