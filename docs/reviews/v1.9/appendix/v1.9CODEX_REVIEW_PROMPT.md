你是资深架构师/审计员。对当前 repo 做 v1.9 基线 Review，必须以 `REVIEW_v1.9_BASELINE.md` 为唯一审计锚点与判断口径，并严格遵守 `v1.9_constraints.md` 的 Non-Interference Contract，以及 `v1.9_roadmap.md` 的 DoD。

目标：
1) 给出“平台化演进前”基线诊断：哪些已达成、哪些未达成、阻断点是什么。
2) 找出导致“状态/交付物真相源不稳定”的结构性根因：尤其是 deliverables(keys) 与 status 字段不一致、409 not_ready / missing pack_key 等“假失败”来源。
3) 定位 /api 与 /v1 双通道及鉴权语义割裂点，给出收敛策略（只保留一条稳定前端路径或做明确兼容桥）。
4) 给出可落地的工程修复方案：用最小改动收敛写入边界（repo.upsert 单入口）、建立 deliverables 事实优先的派生状态策略、并保证 v1.85 主链路不回退。
5) 输出 PR 切分建议（至少 PR-1/PR-2/PR-3），每个 PR 给出：改动范围、文件清单、回归证据、回滚点、Gate 检查项。

必须产出：
- `REVIEW_REPORT_v1.9.md`（放 repo 根目录），结构固定如下：
  0. Executive Summary（一句话结论 + 风险等级）
  1. Baseline 对照（逐条引用 REVIEW_v1.9_BASELINE.md 的要点，对照代码现状）
  2. Non-Interference Contract 检查（逐条检查 v1.9_constraints.md 的 must-not-break）
  3. 路由与鉴权语义审计（列出 /api vs /v1 所有相关路由、调用方、返回差异、401/200 并存原因）
  4. Deliverables 真相源审计
     - 列出 deliverables keys（pack_key/publish_key/…）在哪里写、在哪里读
     - 列出 status/pack_status/publish_status/last_step 在哪里写、在哪里读
     - 画出“写入边界图”：哪些路径会覆盖/回退
     - 给出“单一真相源”方案（deliverables 事实优先；状态派生）
  5. repo.upsert 写入边界收敛方案
     - 当前散落写入点列表（文件+行号）
     - 建议的单入口（例如 TaskRepository/TaskStateWriter/DeliverablesIndexWriter）
     - 幂等/重跑保证与观测字段口径
  6. 与 v1.9 Roadmap 的 DoD 对齐（ScenePack/TaskPlan/AssetLibrary/Pack增强）
  7. PR 切分与 Gate（每个 PR：目的、改动、风险、回滚、验收脚本/日志证据）
  8. 附录：证据索引（引用 .review_artifacts/v1.9/*.txt 里的命中点）

硬约束：
- 不允许建议“直接重写大模块/推翻 v1.85 主链路”。
- 任何触及旧路由语义的改动必须改为：新增可选字段 或 新路由（带版本前缀）。
- 必须给出最小可行的收敛路线：先真相源与写入边界，再 UI 优化，再真实接入。

执行方式：
- 先只读扫描并引用证据（文件+行号）。
- 再提出改动建议（但本次只要求 review，不直接改代码，除非发现阻断级 bug）。
